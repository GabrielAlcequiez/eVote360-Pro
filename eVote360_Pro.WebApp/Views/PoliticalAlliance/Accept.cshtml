@model eVote360_Pro.Core.Application.DTOs.PoliticalAlliance.PoliticalAllianceGetDto
@{
    ViewData["Title"] = "Aceptar Solicitud de Alianza";
}

<div class="container my-4">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card shadow-sm border-0 bg-white">
                <div class="card-body p-4">
                    <div class="d-flex align-items-center mb-3">
                        <i class="bi bi-check-circle-fill text-success fs-3 me-3"></i>
                        <h1 class="h4 mb-0 text-dark">Confirmar Aceptación</h1>
                    </div>
                    <p class="text-muted mb-4">
                        ¿Está seguro de que desea <strong>aceptar</strong> la solicitud de alianza del partido <strong>@Model.RequesterPartyName (@Model.RequesterPartyAcronym)</strong>?
                    </p>
                    <p class="text-muted small mb-4">
                        Al aceptar, se establecerá una alianza vigente entre ambos partidos, lo que permitirá asignar candidatos cruzados entre los partidos aliados.
                    </p>

                    <form asp-action="Accept" asp-route-id="@Model.Id" method="post">
                        <div class="d-flex justify-content-between align-items-center">
                            <a asp-action="Index" class="btn btn-outline-secondary">Cancelar</a>
                            <button type="submit" class="btn btn-success px-4">
                                <i class="bi bi-check-lg me-1"></i> Sí, aceptar
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
